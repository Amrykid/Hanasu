<metro:MetroWindow x:Class="Hanasu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:stations="clr-namespace:Hanasu.Services.Stations"
        Title="Hanasu" Height="410" Width="715">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colours.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.AnimatedSingleRowTabControl.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Red.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="256*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid Height="40" Margin="0,6,12,0" VerticalAlignment="Top" Panel.ZIndex="100" Name="NowPlayingGrid" HorizontalAlignment="Right" Width="341" Visibility="Hidden">
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="19" >Now Playing:</Label>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Name="SongDataLbl" TextWrapping="Wrap" Width="240"></TextBlock>
                    </StackPanel>
                </StackPanel>
            </Grid>
            <TabControl Name="tabControl1" Panel.ZIndex="50">
                <TabItem Header="Now Playing" Name="tabItem1">
                    <Grid>
                        <!--<MediaElement Name="MediaPlayer" LoadedBehavior="Manual" UnloadedBehavior="Manual" />-->
                        <WindowsFormsHost Name="windowsFormsHost1">

                        </WindowsFormsHost>
                    </Grid>
                </TabItem>
                <TabItem Header="Stations">
                    <Grid>
                        <ListView Foreground="Black" Name="StationsListView" ItemsSource="{Binding Source={x:Static stations:StationsService.Instance}, Path=Stations, Mode=OneWay, IsAsync=True, UpdateSourceTrigger=PropertyChanged}" MouseDoubleClick="StationsListView_MouseDoubleClick">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Station" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="Format" DisplayMemberBinding="{Binding Format}"/>
                                    <GridViewColumn Header="City" DisplayMemberBinding="{Binding City}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
        <Grid Grid.Row="1">
            <Border BorderBrush="Black" BorderThickness="1" CornerRadius="7 7 0 0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Style="{DynamicResource MetroCircleButtonStyle}" Name="revBtn">
                            <Image Source="pack://application:,,,/Hanasu;component/Resources/rew.png"/>
                        </Button>
                        <Button Style="{DynamicResource MetroCircleButtonStyle}" Name="playBtn" Click="playBtn_Click">
                            <Image Source="pack://application:,,,/Hanasu;component/Resources/play.png"/>
                        </Button>
                        <Button Style="{DynamicResource MetroCircleButtonStyle}" Name="pauseBtn" Click="pauseBtn_Click">
                            <Image Source="pack://application:,,,/Hanasu;component/Resources/pause.png"/>
                        </Button>
                        <Button Style="{DynamicResource MetroCircleButtonStyle}" Name="ffBtn">
                            <Image Source="pack://application:,,,/Hanasu;component/Resources/ff.png"/>
                        </Button>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Right" Orientation="Vertical" Margin="0 0 10 0">
                        <Label Margin="0 -10 0 0" FontSize="16">Volume</Label>
                        <Slider Margin="0 -10 0 0" Name="VolumeSlider" Height="35" Width="90" ValueChanged="VolumeSlider_ValueChanged" Maximum="100" Minimum="0" TickFrequency="5"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</metro:MetroWindow>
